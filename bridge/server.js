# Cursor Bridge — placeholder
# Мост: поднять WSS + TLS, keeper для `agent`, pairing QR.
# См. README в корне репозитория.

console.error('cursor-bridge: skeleton only. Implement server next.');
process.exit(1);
