//! cursor-tunnel: TCP-over-iroh sidecar (skeleton).
//! Target API mirrors claude-tunnel: listen | connect | id.

fn main() {
    eprintln!("cursor-tunnel: skeleton. Port claude-tunnel logic next.");
    std::process::exit(1);
}
